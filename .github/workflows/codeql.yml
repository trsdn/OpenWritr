name: CodeQL

# Advanced setup instead of GitHub's default setup: the default setup's Swift
# autobuild failed on this package, and building with the same command as CI
# (swift build -c release -Xswiftc -warnings-as-errors) is deterministic and
# analyzes exactly what CI and the release build. Criterion P13.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    # The Swift analysis builds the package and takes about half an hour, so
    # pull requests run it only when code or workflows change. Pushes to main and
    # the weekly schedule always run it.
    paths:
      - Sources/**
      - Tests/**
      - Package.swift
      - Package.resolved
      - scripts/**
      - .github/workflows/**
  schedule:
    - cron: "41 3 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: macos-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: python
            build-mode: none
          - language: swift
            build-mode: manual
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Initialize CodeQL
        uses: github/codeql-action/init@1c5b675653bb5c22dbe9b12b556ec555138e09fd # v4.38.1
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Build (Swift)
        if: matrix.language == 'swift'
        run: swift build -c release -Xswiftc -warnings-as-errors

      - name: Analyze
        uses: github/codeql-action/analyze@1c5b675653bb5c22dbe9b12b556ec555138e09fd # v4.38.1
        with:
          category: "/language:${{ matrix.language }}"
