name: Repository stats

on:
  workflow_dispatch:
  schedule:
    - cron: "23 5 * * *"
  push:
    branches:
      - main
    paths:
      - .github/workflows/stats.yml

permissions:
  contents: write

jobs:
  stats:
    uses: trsdn/.github/.github/workflows/repo-stats.yml@56a792f661ea87ecd98fbe7b11e292690e8fc721 # v1.21.0
    with:
      generator-ref: 56a792f661ea87ecd98fbe7b11e292690e8fc721
      output-dir: .github/stats
      theme: both
      cards: repo-card
      # main is protected, so the cards are committed to a generated branch.
      branch: stats
      commit-message: "chore(stats): update repository stats"
    secrets:
      STATS_TOKEN: ${{ secrets.STATS_TOKEN }}
